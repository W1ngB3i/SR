{
  "name": "@sr/shared",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "SR 审核系统前后端共享契约：DTO、枚举、API 路径与校验 schema",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^5.0.1"
  }
}
